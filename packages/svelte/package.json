{
  "name": "svelte-app",
  "version": "1.0.0",
  "private": true,
  "dependencies": {
    "lodash": "^4.17.15"
  },
  "scripts": {
    "build": "rollup -c",
    "autobuild": "rollup -c -w",
    "dev": "run-p start:dev autobuild",
    "start": "sirv public --single",
    "start:dev": "sirv public --single --dev",
    "clean": "rimraf public server/build"
  }
}
