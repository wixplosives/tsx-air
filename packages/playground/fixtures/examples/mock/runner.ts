import { render } from '@tsx-air/framework';
import { FromSource } from './index.source';

export const runExample = (_: HTMLElement) => {
    if (typeof render === 'function') {
        //
    }
};

export { FromSource as Component};