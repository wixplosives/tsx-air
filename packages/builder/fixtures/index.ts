import { join } from "path";

export const base = __dirname;
export const browserify = join(base, 'browserify');
export const breakpoints = join(base, 'breakpoints');
export const build = join(base, 'build');